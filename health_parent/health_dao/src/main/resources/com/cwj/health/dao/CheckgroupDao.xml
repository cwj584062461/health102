<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cwj.health.dao.CheckgroupDao">
    <!--添加检查组-->
    <insert id="add" parameterType="com.cwj.health.pojo.CheckGroup" keyProperty="id" useGeneratedKeys="true">
        insert into t_checkgroup (code,name,helpCode,sex,remark,attention) values (#{code},#{name},#{helpCode},#{sex},#{remark},#{attention})
    </insert>

    <!--添加检查组与检查项的关系-->
    <insert id="addCheckGroupCheckItem" parameterType="int">
        insert inot t_checkgroup_checkitem (checkgroup_id,,checkitem_id) values (#{checkGroupId},#{checkitemId})
    </insert>

    <!--分页查询-->
    <select id="findPage" parameterType="String" resultType="com.cwj.health.pojo.CheckGroup">
        select * from t_checkgroup
        <if test="value != null and value.length>0">
            where code like #{value} or name like #{value}
        </if>
    </select>
</mapper>